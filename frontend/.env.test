# Vitest / CI — keep unit tests on mock services (no live backend required).
# Does not affect `npm run dev` (uses `.env` with REST).
VITE_CHAT_SERVICE_MODE=mock
VITE_API_BASE_URL=/api
VITE_SOCKET_URL=
