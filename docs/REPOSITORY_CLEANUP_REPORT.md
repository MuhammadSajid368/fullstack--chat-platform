# Repository Cleanup Report

Date: 2026-07-28

## Scope

This report audits repository hygiene and production-readiness for public GitHub publication.

Constraints followed:

- no business logic changes
- no API changes
- no architecture refactors
- no automatic deletions
- only `.gitignore` was updated because the ignore additions are low-risk and unquestionably correct

## Repository Size Overview

- Files present: `53,740`
- Directories present: `6,653`
- Approximate working tree size: `1,238,265,758` bytes (`~1.15 GiB`)

Important context:

- `git ls-files` returned no tracked files
- `git status --short` showed the entire tree as untracked

That means the repository is currently being evaluated as a working tree that has not been curated yet for a public first commit.

## Verified Production Artifacts That Must Remain

These should stay tracked.

### Core project files

- `README.md`
- `.gitattributes`
- `.gitignore`

### CI / automation

- `.github/workflows/ci.yml`

### Deployment / infra

- `docker-compose.dev.yml`
- `docker-compose.prod.yml`
- `backend/Dockerfile`
- `frontend/Dockerfile`
- `deploy/nginx/`
- `deploy/redis/`
- `deploy/prometheus/`
- `deploy/grafana/`
- `deploy/scripts/`
- `deploy/certs/.gitkeep`

### Environment templates

- `.env.production.example`
- `.env.staging.example`
- `backend/.env.example`
- `frontend/.env.example`

### Backend source-of-truth assets

- `backend/prisma/`
- `backend/tests/`
- `backend/docs/api-verification/`
- `backend/src/`

### Frontend source-of-truth assets

- `frontend/src/`
- `frontend/public/`
- `frontend/docs/`
- `frontend/.env.test`

Reason `frontend/.env.test` stays:

- it is explicitly documented in `frontend/docs/DEV_AUTH.md`
- it is used to keep Vitest on mock mode

### Documentation

- `docs/DEPLOYMENT.md`
- `docs/DOCKER.md`
- `docs/OPERATIONS.md`
- `docs/BACKUP_RESTORE.md`
- `docs/ENV_REFERENCE.md`

## Sensitive Files

These should never be committed to a public repository.

### High Risk

- `backend/.env`
  - contains a concrete PostgreSQL password in `DATABASE_URL`
  - contains `JWT_ACCESS_SECRET`
  - contains `JWT_REFRESH_SECRET`

### Medium Risk

- `frontend/.env`
  - local runtime env file; not appropriate for a public repo

- `frontend/.env.prod.ci`
  - contains secret-like values including:
    - `POSTGRES_PASSWORD`
    - `JWT_ACCESS_SECRET`
    - `JWT_REFRESH_SECRET`
    - `METRICS_TOKEN`
    - `GRAFANA_ADMIN_PASSWORD`

### Data Exposure Risk

- `dump.rdb`
  - Redis persistence artifact
  - may contain local runtime state

### Verified Safe

- No live `*.pem`, `*.key`, `*.crt`, `*.cer`, `*.p12`, or `*.pfx` files were found
- No live `deploy/prometheus/bearer.token` was found
- `deploy/prometheus/bearer.token.example` is safe to keep

## Generated Files and Artifacts

These are generated or machine-produced artifacts. Do not commit them.

### Present now

- `backend/node_modules/`
- `frontend/node_modules/`
- `backend/dist/`
- `dump.rdb`

### Not present now, but relevant to this repo

- `coverage/`
- `.vite/`
- `.vitest/`
- `.nyc_output/`
- `playwright-report/`
- `test-results/`
- `*.tsbuildinfo`
- `*.log`
- `*.pid`
- `*.out`
- `*.err`
- `*.aof`

### Generated but intentionally keep

These are generated documentation artifacts, not runtime junk:

- `backend/docs/api-verification/Chat-API.postman_collection.json`
- `backend/docs/api-verification/Chat-API.postman_environment.json`

Reason:

- they are linked from docs
- they are part of the API verification pack
- they are regenerated by `backend/docs/api-verification/generate-postman.mjs`

## Files Safe To Ignore

These patterns are now covered by `.gitignore`.

### Environment and CI files

- `.env`
- `.env.local`
- `.env.ci`
- `.env.prod.ci`
- `.env.development`
- `.env.production`
- `.env.production.local`
- `.env.staging`
- `**/.env`
- `**/.env.local`
- `**/.env.ci`
- `**/.env.prod.ci`
- `**/.env.development`
- `**/.env.production`
- `**/.env.production.local`
- `**/.env.staging`

Exceptions intentionally preserved:

- `.env.production.example`
- `.env.staging.example`
- `**/.env.example`
- `frontend/.env.test`

### Secrets and certificate material

- `**/deploy/prometheus/bearer.token`
- `**/deploy/certs/**`
- `*.pem`
- `*.key`
- `*.crt`
- `*.cer`
- `*.p12`
- `*.pfx`

### Dependency and build output

- `node_modules/`
- `**/node_modules/`
- `dist/`
- `build/`
- `coverage/`
- `**/dist/`
- `**/build/`
- `**/coverage/`
- `*.tsbuildinfo`

### Tooling caches and reports

- `.vite/`
- `.vitest/`
- `.nyc_output/`
- `.turbo/`
- `.next/`
- `.cache/`
- `tmp/`
- `temp/`
- `playwright-report/`
- `test-results/`

### Runtime and OS clutter

- `*.log`
- `*.out`
- `*.err`
- `*.pid`
- `*.seed`
- `*.tgz`
- `.DS_Store`
- `Thumbs.db`
- `Desktop.ini`
- `.idea/`
- `.vscode/settings.json`
- `*.swp`
- `*.swo`
- `*.rdb`
- `*.aof`
- `backups/`
- `**/backups/**`

## Files Safe To Remove

These are high-confidence removal candidates from a public-repo perspective.

### Runtime / secret artifacts

- `dump.rdb`
  - Risk: High
  - Why: runtime-generated Redis snapshot with no repo references

- `backend/.env`
  - Risk: High
  - Why: local secret-bearing env file; should not be published

- `frontend/.env`
  - Risk: Medium
  - Why: local env file; should not be published

- `frontend/.env.prod.ci`
  - Risk: High
  - Why: unreferenced local CI/prod secret artifact
  - Evidence:
    - `.github/workflows/ci.yml` generates `.env.prod.ci` at repo root
    - no repo references to `frontend/.env.prod.ci`

### Dead frontend files

- `frontend/src/services/mockChatService.ts`
  - Risk: Low
  - Classification: SAFE TO REMOVE
  - Evidence: no imports/usages found

- `frontend/src/services/chatService.ts`
  - Risk: Low
  - Classification: SAFE TO REMOVE
  - Evidence: no inbound imports found

- `frontend/src/services/index.ts`
  - Risk: Low
  - Classification: SAFE TO REMOVE
  - Evidence: no imports from `services/index`

- `frontend/src/utils/axios.ts`
  - Risk: Low
  - Classification: SAFE TO REMOVE
  - Evidence: no imports/usages; comment already points to replacement client

### Dead backend files

- `backend/src/modules/**/index.ts` barrel files, except `backend/src/modules/auth/utils/index.ts`
  - Risk: Medium
  - Classification: SAFE TO REMOVE
  - Evidence:
    - repo-wide searches found no consumers for these module barrel files
    - concrete files are imported directly elsewhere
    - `backend/src/modules/auth/utils/index.ts` is the exception and is actively used

### Stale documentation

- `docs/PRODUCTION_READINESS_AUDIT.md`
  - Risk: Low
  - Classification: SAFE TO REMOVE
  - Evidence:
    - no repo references found
    - dated audit snapshot rather than living operational documentation

## Files Requiring Manual Review

These should be reviewed by a human before removal.

### Backend docs

- `backend/README.md`
  - Risk: Medium
  - Why:
    - clearly stale
    - claims business API endpoints are not implemented
    - but it is still a legitimate repository document
  - Recommendation: update, do not delete blindly

- `backend/ARCHITECTURE.md`
  - Risk: Medium
  - Why:
    - clearly stale in places
    - still useful as architecture documentation
  - Recommendation: update, do not delete blindly

### Frontend data exports

- `frontend/src/data/index.ts`
  - Risk: Medium
  - Why:
    - file is live
    - some exports appear unreferenced
    - but the file itself is used by active UI components
  - Recommendation: prune unused exports only after targeted confirmation

### Test fixture policy

- `frontend/.env.test`
  - Risk: Low
  - Why:
    - env files are usually excluded from Git
    - this one is intentionally documented and used
  - Recommendation: keep unless you choose a stricter “no env files at all” policy

## Frontend Audit Highlights

### Safe To Remove

- `frontend/src/services/mockChatService.ts`
- `frontend/src/services/chatService.ts`
- `frontend/src/services/index.ts`
- `frontend/src/utils/axios.ts`

### Keep

- all current route-loaded pages
- current hooks
- mock services/data that still participate in tests
- `frontend/.env.test`

### Manual Review

- stale exports in `frontend/src/data/index.ts`

## Backend Audit Highlights

### Safe To Remove

- unused module barrel files under `backend/src/modules/**/index.ts`
  - except `backend/src/modules/auth/utils/index.ts`

### Keep

- runtime modules, routes, DTOs, validators, repositories, services, and tests that are imported directly
- API verification artifacts under `backend/docs/api-verification/`
- Prisma schema and data-model docs

### Manual Review

- `backend/README.md`
- `backend/ARCHITECTURE.md`

## Package Dependency Review

### Likely Unused Frontend Dependencies

- `emoji-mart`
  - Risk: Low
  - Evidence: scoped `@emoji-mart/*` packages are used, but no import from `emoji-mart`

- `prop-types`
  - Risk: Low
  - Evidence: no `prop-types` import or `PropTypes` usage found

- `@testing-library/user-event`
  - Risk: Low
  - Evidence: no `userEvent` usage found

### Likely Unused Backend Dependencies

- `@opentelemetry/context-async-hooks`
  - Risk: Medium
  - Evidence: no imports or references found

- `@opentelemetry/instrumentation-express`
  - Risk: Medium
  - Evidence: no imports or references found

- `@opentelemetry/instrumentation-http`
  - Risk: Medium
  - Evidence: no imports or references found

- `@opentelemetry/instrumentation-ioredis`
  - Risk: Medium
  - Evidence: no imports or references found

### Manual Review Dependencies

- `@vitest/coverage-v8`
  - Risk: Low
  - Reason: no direct repo references, but may be invoked only through CLI flags

### Overlap Notes

- `emoji-mart` overlaps with `@emoji-mart/data` and `@emoji-mart/react`
- `@testing-library/react` and `@testing-library/jest-dom` are used, but would be more naturally placed in `devDependencies`

## Recommended `.gitignore` Additions

Applied to root `.gitignore`:

- env and CI env patterns
- cert and bearer-token exclusions
- Node/Vite/Vitest/TypeScript build outputs
- logs and runtime files
- Redis persistence artifacts
- editor and OS clutter

## Summary

### ✅ Files safe to ignore

- local env files
- CI-generated env files
- dependency folders
- build output
- coverage and test reports
- logs and runtime state
- Redis dumps
- cert/key material
- editor and OS junk

### ✅ Files safe to remove

- `dump.rdb`
- `backend/.env`
- `frontend/.env`
- `frontend/.env.prod.ci`
- `frontend/src/services/mockChatService.ts`
- `frontend/src/services/chatService.ts`
- `frontend/src/services/index.ts`
- `frontend/src/utils/axios.ts`
- unused backend module barrel files under `backend/src/modules/**/index.ts`
- `docs/PRODUCTION_READINESS_AUDIT.md`

### ⚠ Files requiring manual review

- `backend/README.md`
- `backend/ARCHITECTURE.md`
- `frontend/src/data/index.ts`
- `frontend/.env.test`
- `@vitest/coverage-v8`
- OpenTelemetry instrumentation packages

### ❌ Files that must remain in the repository

- Dockerfiles
- `docker-compose.*`
- `deploy/`
- nginx config
- Prometheus config
- Grafana dashboards/provisioning
- `.github/workflows/ci.yml`
- Prisma schema and migrations
- `README.md`
- env example files
- API verification pack
- active docs and deployment scripts
